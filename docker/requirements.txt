pandas==2.2.*
sqlalchemy==2.0.*
psycopg2-binary==2.9.*
duckdb==1.0.*
pyduckdb==0.10.*         # optional; pandas uses duckdb directly too
requests==2.32.*
tenacity==8.5.*          # retry for APIs
pydantic==2.8.*          # model API payloads (optional)
openpyxl==3.1.*          # Excel reader
xlrd==2.0.*              # if xls ever appears
apache-airflow-providers-amazon==8.0.0  # example (ignore if not needed)
apache-airflow-providers-http==4.10.0   # HttpHook if you prefer it
great-expectations==0.18.*              # optional DQ framework